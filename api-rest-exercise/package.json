{
	"name": "rest-api-exercise-client",
	"version": "0.0.0",
	"description": "REST api client exercise",
	"main": "index.js",
	"scripts": {
		"start": "npm-run-all -p -l start:dev start:mock-server",
		"start:mock-server": "cd server && npm start",
		"start:dev": "parcel index.html",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"keywords": [
		"REST",
		"api",
		"fetch",
		"promises",
		"axios"
	],
	"author": "Juan Pablo Martínez",
	"license": "MIT",
	"dependencies": {
		"npm-run-all": "^4.1.5",
		"regenerator-runtime": "^0.13.7"
	},
	"devDependencies": {
		"axios": "^0.18.1",
		"parcel": "^1.11.0"
	}
}
